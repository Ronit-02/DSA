#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

// Question: Robber wants to steal max amount of money from non-adj houses,
// given that houses are in circular fashion

// Recursion (req value to base case)
int f1(vector<int> &nums)
{
    
}

// Memoization
int f2()
{
}

// Tabulation
int f3()
{
}

// Space Optimization
int f4()
{
}

int houseRobber(vector<int> &nums)
{
    // vector<int> dp(n + 1, -1);
    return f1(nums);
}

int main()
{
    vector<int> nums = {1, 2, 4};
    cout << houseRobber(nums);

    return 0;
}